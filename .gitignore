# Dependencies
node_modules/

# Build outputs
dist/
build/
coverage/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS files
.DS_Store
Thumbs.db

# IDE/editor
.vscode/
.idea/
*.swp
*.swo

# Environment/local config
.env
.env.local
.env.*.local

# Generated exports and QA output
exports/
release/
releases/
screenshots/generated/
tests/output/
golden-output/
tmp/

# Archives
*.zip
*.tar.gz

# Python helper caches
__pycache__/
*.pyc

# FONT-CAL-001: temporary experimental candidate fonts (must never become production assets --
# only the JSON measurements and the experiment report they support are committed)
tools/font-cal-001/output/fonts/

# TXT-104: ad hoc local verification screenshots/exports for manual visual review -- never a repo
# artifact, not regenerated by any committed script
tools/txt104-verification-screenshots/

# RC-011: ad hoc live-browser Visual QA screenshots (Category 4 gate check) -- same category as
# the TXT-104 folder above, never a repo artifact
tools/rc011-verification-screenshots/

# RC-013: ad hoc live-browser Visual QA screenshots for the plate vertical-flip fix, same category
# as the RC-011 folder above, never a repo artifact
tools/rc013-verification-screenshots/

# RC-014: ad hoc scratch verification scripts + screenshots for the release-gate re-audit, same
# "never a repo artifact" category as the folders above
tools/rc-014-plate-flip-verification.mjs
tools/rc-014-font-spotcheck.mjs
tools/rc-014-verification-screenshots/

# RS-3010 Step 1: ad hoc live-browser verification screenshots for the drawing-board freehand
# tool, same "never a repo artifact" category as the folders above
tools/rs-3010-step1-verification-screenshots/

# RS-3010 Step 2a: ad hoc live-browser verification screenshots for the multi-shape data model,
# rectangle/ellipse presets, and select/move/delete -- same "never a repo artifact" category
tools/rs-3010-step2a-verification-screenshots/

# RS-3010 Step 2b: ad hoc live-browser verification screenshots for the slot (pill/capsule)
# preset -- same "never a repo artifact" category
tools/rs-3010-step2b-verification-screenshots/

# RS-3010 Step 2c: ad hoc live-browser verification screenshots for the polygon
# (click-to-add-vertex) preset -- same "never a repo artifact" category
tools/rs-3010-step2c-verification-screenshots/

# RS-3010 Design Step A: ad hoc live-browser verification screenshots for the new tool rail +
# options panel layout shell -- same "never a repo artifact" category
tools/rs-3010-design-step-a-verification-screenshots/

# RS-3010 Design Step B: ad hoc live-browser verification screenshots for shortcuts/cursor/pan --
# same "never a repo artifact" category
tools/rs-3010-design-step-b-verification-screenshots/

# RS-3010 Design Step C: ad hoc live-browser verification screenshots for marquee-select --
# same "never a repo artifact" category
tools/rs-3010-design-step-c-verification-screenshots/

# RS-3010 Design Step D: ad hoc live-browser verification screenshots for resize handles --
# same "never a repo artifact" category
tools/rs-3010-design-step-d-verification-screenshots/

# RS-3010 Design Step E: ad hoc live-browser verification screenshots for the end-to-end
# integration pass across Design Steps A-D -- same "never a repo artifact" category
tools/rs-3010-design-step-e-verification-screenshots/

# RS-3010 Step 2d: ad hoc live-browser verification screenshots for the Design canvas's own
# background grid -- same "never a repo artifact" category
tools/rs-3010-step2d-verification-screenshots/

# RS-3010 Step 2e: ad hoc live-browser verification screenshots for grid snapping -- same
# "never a repo artifact" category
tools/rs-3010-step2e-verification-screenshots/

# RS-3010 Step 2f: ad hoc live-browser verification screenshots for vertex/point snapping and
# angle snapping -- same "never a repo artifact" category
tools/rs-3010-step2f-verification-screenshots/

# RS-3011 Step 1: ad hoc live-browser verification screenshots for the removed Commit Shape step
# (shapes now land in project.layers immediately) -- same "never a repo artifact" category
tools/rs-3011-step1-verification-screenshots/

# RS-3011 Step 3: ad hoc live-browser investigation screenshots for the live stone panel scoping
# question (Inspector fields vs. Design canvas vs. #designToolOptionsPanel) -- same "never a repo
# artifact" category
tools/rs-3011-step3-investigation-screenshots/

# RS-3011 Step 3a: ad hoc live-browser verification screenshots for mirroring stone fields into
# Design's tool-options panel -- same "never a repo artifact" category
tools/rs-3011-step3a-verification-screenshots/

# RS-3001: throwaway CSS-isolation spike prototypes (copied drawleather source slices +
# harness/comparison scripts) -- findings are captured in
# docs/specifications/RS-3001-css-isolation-addendum.md, the prototype code itself is never a
# repo artifact, same category as the folders above
tools/scratch/rs-3001-css-spike/

# RS-3011 Step 3b: throwaway native-Paper.js-stone-rendering feasibility spike (harness page +
# measurement scripts + screenshots) -- findings reported directly to the user, the prototype
# code itself is never a repo artifact, same category as the folder above
tools/scratch/rs-3011-step3b-spike/

# RS-3011 mode-toggle-bugs fix: ad hoc live-browser Playwright verification script + screenshots
# for the setDrawTool()/commitFinalizedShape() mode-toggle fixes, same "never a repo artifact"
# category as the folders above
tools/rs-3011-fix-mode-toggle-verify.mjs
tools/rs-3011-fix-mode-toggle-screenshots/

# RS-3011 Step 3b: ad hoc live-browser verification script + screenshots for the live per-shape
# stone preview on the Design canvas -- same "never a repo artifact" category as the folders above
tools/rs-3011-step3b-verify.mjs
tools/rs-3011-step3b-verification-screenshots/

#RS-3011 verification-artifact
tools/rs-3011-canvas-desync-verify.mjs
tools/rs-3011-canvas-desync-verification-screenshots/

# RS-3011 resize-perf-spike: profiling-only investigation of rebuildStoneGroupForShape()'s
# ~35ms/step resize-drag cost -- throwaway instrumentation/prototype code + screenshots, never a
# repo artifact, same "never a repo artifact" category as the folders above
tools/scratch/rs-3011-resize-perf-spike/

# RS-3011 fix-resize-perf-partial: throwaway verification scripts (behavioral + re-run of the
# spike's own CDP-trace methodology) for confirming the hide-during-drag fix, same "never a repo
# artifact" category as the spike folder above.
tools/scratch/rs-3011-fix-verify/

#RS-3011 verification-artifact: open-contour stone-sampling fix
tools/rs-3011-open-contour-verify.mjs
tools/rs-3011-open-contour-verification-screenshots/

# RS-3011 Step 9: ad hoc live-browser Playwright verification script + screenshots for the Pen
# (Bezier) tool, same "never a repo artifact" category as the scripts above
tools/rs-3011-step9-pen-tool-verify.mjs
tools/rs-3011-step9-pen-tool-verification-screenshots/

# RS-3011 Step 9 follow-up: ad hoc live-browser Playwright verification script + screenshots for
# finishOpenPenPath() (double-click-to-finish an open Pen path), same "never a repo artifact"
# category as the scripts above
tools/rs-3011-pen-finish-open-path-verify.mjs
tools/rs-3011-pen-finish-open-path-verification-screenshots/

# RS-3011 corner-gap-spike: investigation-only repro/measurement scripts for the dedupeStonePoints()
# corner-drop gap issue (issue #5 of the prior six-bug investigation) -- throwaway, never a repo
# artifact, same "never a repo artifact" category as the spike folders above
tools/scratch/rs-3011-corner-gap-spike/

# RS-3011 pen-circle-verify: ad hoc live-browser Playwright verification script + screenshots for
# manually reviewing a 4-anchor cubic-bezier circle built with the Pen tool's curved segments and
# curved closing drag, step by step -- manual visual-review exercise, no application code changes,
# same "never a repo artifact" category as the scripts above
tools/rs-3011-pen-circle-verify.mjs
tools/rs-3011-pen-circle-verification-screenshots/

# RS-3011 pen-anchor-handle-visuals: ad hoc live-browser Playwright verification script +
# screenshots for the new anchor-dot/handle-tangent/handle-tip chrome (visible-first-anchor,
# mid-drag tangent+tip, multi-anchor simultaneous chrome, chrome removed on close, chrome removed on
# Escape), same "never a repo artifact" category as the scripts above
tools/rs-3011-pen-anchor-handle-visuals-verify.mjs
tools/rs-3011-pen-anchor-handle-visuals-verification-screenshots/

#RS-3011: gitignore ad hoc Pen skip-move-hittest verification script
tools/rs-3011-pen-skip-move-hittest-verify.mjs
tools/rs-3011-pen-skip-move-hittest-verification-screenshots/

# RS-3011 Step 4: ad hoc live-browser Playwright verification script for the persisted-active-view
# boot logic (scenarios a-g: fresh-boot default, tab-switch persistence, Design content+view
# restore, narrow-viewport override without clobbering the saved preference, and a boot-time vs.
# click-triggered Design entry canvas-sizing comparison) -- same "never a repo artifact" category
# as the scripts above
tools/rs-3011-step4-verify.mjs

# RS-3011 Step 4: ad hoc CDP-screencast Playwright scripts that caught and then re-verified the
# fix for a real ~25-30ms visible flash of Dual Workspace during the boot-time 'design' path's
# settle-generation step (plain default-project case + a heavier 7-layer case), plus their captured
# evidence frames -- same "never a repo artifact" category as the scripts above
tools/rs-3011-step4-flash-check.mjs
tools/rs-3011-step4-flash-check-heavy.mjs
tools/rs-3011-step4-flash-check-screenshots/

# RS-3011 nav-toggle fix: ad hoc live-browser Playwright verification script for the
# #menuDesign-no-longer-toggles + Lightbox-openers-reveal-Dual-Workspace change (scenarios a-f),
# plus its captured screenshots -- same "never a repo artifact" category as the scripts above
tools/scratch/rs-3011-fix-design-nav-toggle/

# RS-3011 Step 10b Phase B: ad hoc live-browser Playwright verification script for the Paint tool's
# lasso interaction/target-selection/region-creation flow, plus its captured screenshots -- same
# "never a repo artifact" category as the scratch folders above
tools/scratch/rs-3011-step10b-paint-phaseb/

# RS-3011 Step 10b bug investigation: ad hoc live-browser Playwright diagnostic script for the
# reported undo/status/multi-region bugs, same "never a repo artifact" category as the scratch
# folders above
tools/scratch/rs-3011-step10b-bug-investigation/

# RS-3011 Step 5: ad hoc live-browser Playwright verification scripts for the empty-grid
# investigation, objectTemplateDetail de-prominence and layoutStats de-confusion work, plus their
# captured screenshots -- same "never a repo artifact" category as the scratch folders above
tools/scratch/rs-3011-step5-verify/

# RS-3011 Step 6: ad hoc live-browser Playwright verification script for the layer-naming-by-
# shape-type work, plus its captured screenshot -- same "never a repo artifact" category as the
# scratch folders above
tools/scratch/rs-3011-step6-verify/

# RS-3011 Step 7: ad hoc live-browser Playwright verification script for button-gated stone
# generation on Design-drawn shapes, plus its captured screenshots -- same "never a repo artifact"
# category as the scratch folders above
tools/scratch/rs-3011-step7-verify/

# RS-3011 Step 8 Phase B: ad hoc live-browser Playwright verification script + SVG fixtures +
# screenshots for the Import SVG action inside Design (button-gated stone generation, multi-contour
# CompoundPath rendering, mismatched open/closed warning surfaced in #status), same "never a repo
# artifact" category as the scratch folders above
tools/scratch/rs-3011-step8-phaseb-verify/

# RS-3011 freehand-close-and-clear-all-layers fix: ad hoc live-browser Playwright verification
# script + screenshots for freehand's new close-on-return-to-start gesture and deleteLayer()'s new
# blank-text-layer-on-last-delete behavior, same "never a repo artifact" category as the scratch
# folders above
tools/scratch/rs3011-freehand-close-clear-layers-verify/
