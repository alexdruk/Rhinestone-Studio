# Backlog

This file predates the current per-milestone specification process
(`docs/specifications/`) and is not actively maintained per-commit. For current implementation
status, see `docs/ARCHITECTURE.md` (authoritative, updated per milestone) rather than this table.

Every item below was part of the Version 1.0 scope; Version 1.0 is now formally released (`RC-008`,
closed after the `RC-002`–`RC-007` stabilization series and `ARCH-REVIEW-001`'s architecture
review found no open release-blocking defect).

|Priority|Feature|Status|
|---|---|---|
|P0|Curved text|Done (RS-1003)|
|P0|SVG import|Done (RS-1001)|
|P0|Multi-object support|Done (RS-1004)|
|P1|Undo/Redo|Done (RS-1002)|

## Known data defects (not release-blocking, deferred)

|Item|Found by|Notes|
|---|---|---|
|`assets/fonts/Montserrat-Regular.ttf` is Montserrat **Thin**, not Regular|READ-003|**Resolved by FONT-LIB-005:** the library ships without Montserrat. The bundled file is byte-identical to Google Fonts' official *variable* Montserrat (the `Montserrat[wght].ttf` file under their OFL directory), whose `wght` axis defaults to 100 — so `opentype.js` renders its default instance as Thin. Google publishes no static Regular to swap in; producing one means instancing the variable font at `wght=400`, which needs Python `fontTools` for a single binary. Not worth it — Poppins already covers the modern-sans role at 400/600/700. The manifest now carries `montserrat-regular` as `"style": "Thin"`, `"weight": 100`, `"enabled": false`. The `.ttf` is **retained** so projects that already use Montserrat keep rendering byte-identically. See `docs/specifications/FONT-LIB-005-MontserratRetired.md`. Original finding: the bundled file reports `usWeightClass = 100` (Anton and Great Vibes both report 400). It ships under the `montserrat-regular` id and a "Regular" style label, so anyone picking Montserrat in the font library silently gets a hairline weight — its measured `stemWidthRatio` (0.0145) is the lowest in the library by a wide margin. Independent of readability.|

## Deferred technical follow-ups

A row closed by a later milestone keeps its original finding text and gains a bold `Resolved by <MILESTONE>:` prefix in the Notes cell.

|Item|Found by|Notes|
|---|---|---|
|`sampleRadialFillPoints()` per-component anchors do not extend to `sampleRadialFieldFillPoints()`|READ-002|The vector radial sampler now rays out from one anchor per connected component (`groupPolygonsIntoComponents()`), fixing the "bullseye in the middle, straight rows at the edges" defect for multi-part text/SVG. `sampleRadialFieldFillPoints()` (image/raster layers, `GeometryEngine.generateImageLayout()`) still uses a single whole-placement anchor and has the same defect, but a raster density field has no polygon contours to group — it needs raster connected-component (blob) labelling of the density mask, a different technique. Left untouched by READ-002.|
|`groupPolygonsIntoComponents()` is O(n²) in contour count|READ-002|Pairwise containment test with a bounding-box prefilter, then one `isPointInsidePolygons()` per surviving pair. Measured wall time: Cinzel "Vitalina" (45 contours) 0.14 ms; Cinzel "Vitalina Katarina" (93 contours) 0.33 ms. Untested above ~90 contours; revisit with a spatial index or interval tree if a large SVG import makes radial fill noticeably slow.|
|Letter spacing is unexposed — `letterSpacingMm` exists in `GeometryEngine` but appears zero times in `app.js`|READ-005A|**Resolved by READ-006:** letter spacing is a live text control. `#letterSpacing` is wired in `index.html`; `letterSpacingBoundsMm()` derives its pitch-based min/max, `writeSelectedControlsToLayer()` clamps `l.letterSpacing` on write, and the "Separate letters" button solves and writes a per-rung value. The `autoFit` interaction the original finding wanted a milestone for is handled by the tracked-width guard at `app.js:4197`. The original count was also wrong (measured on `develop` at this commit): `letterSpacingMm` appears **7** times in `app.js` and `letterSpacing` in any form **35** times — e.g. the READ-006 comment at `app.js:601` on `#letterSpacing`'s pitch-derived bounds — not zero. Original finding: the only validated quality improvement from the calibration work (McNemar p = 0.0078, 8 fixes / 0 harms), costing median +26% width; needs its own milestone for the `autoFit` interaction.|
|Staggered and radial sell at 9% and 10% at every ratio tested|READ-005A|No ratio floor rescues them; the open question is whether the interior samplers have a geometry defect or the modes should stop being offered for text. See `docs/specifications/READ-005A-CalibrationFindings.md`.|
|The READ-010 warn-only readability floor does not apply to `providerId: 'rhinestone'` fonts|READ-011C|`textHeightBelowReadableMinimum()` (`app.js`) gates on `layer.height ÷ layer.stoneSize`, but `RhinestoneFontProvider` (`src/text/rhinestoneFont/RhinestoneFontProvider.js`) does not scale authored stone positions by `heightMm` — the READ-011C audit measured a constant 21.40 mm ink height and constant stone count for `rs-block` / `rs-modern` across every specified height. So the below-floor warning can fire when the rendering is actually fine and stay silent when it is not, and `#heightFixToFloorBtn` "fixes" it by writing a new `layer.height` that clears the warning while leaving the stone output byte-identical. This affects `rs-block`, the default Production Font. The authored-pitch floor rule for these fonts (READ-011A §5) is the real fix and its own milestone.|
|`rsBlock.js` / `rsModern.js` hardcode `PITCH_MM = 3.1` with no stone-size awareness|READ-011C|**Resolved by FONT-PITCH-001:** the overlapping pairing is no longer reachable through the Stone Size picker — `rs-block` / `rs-modern` now carry `unsupportedStoneSizes: ["ss16", "ss20", "ss30"]` in `assets/fonts/manifest.json`, so `updateStoneSizePrintableCapabilityUI()` disables those options for an authored-font text layer and `updateStoneSizeOverlapCapabilityUI()` explains the fixed-grid cause for a project that already holds such a pairing. `PITCH_MM` is unchanged — 3.1 mm is the font's authored design, not a defect, and `tools/test-font-pitch-001-authored-stone-sizes.mjs` derives the excluded set from the real glyph maps + kerning so the manifest can never drift from it. Original finding: `src/text/rhinestoneFont/families/rsBlock.js` and `src/text/rhinestoneFont/families/rsModern.js` place every authored stone at `col * PITCH_MM` with `PITCH_MM = 3.1` (SS10's 2.8 mm stone + 0.3 mm gap). `GeometryEngine` then stamps each stone with the layer's selected `stoneSizeMm`, so pairing a Production Font with SS16 (4.0 mm) or SS20 (4.7 mm) places overlapping stones — verified: `rs-block` "Vitalina" holds a 3.100 mm nearest-neighbour at every size, giving 64 overlapping pairs at SS16 and 87 at SS20. Nothing in the pitch is stone-size-derived. READ-011C investigated (no code change) whether the UI prevents that pairing: there is **no `providerId: 'rhinestone'`-specific stone-size constraint**. `updateStoneSizePrintableCapabilityUI()` gates only on printable height and the manifest `unsupportedStoneSizes` list (empty for both authored fonts). The general overlap sweep `updateStoneSizeOptionAvailabilityUI()` *does* generate each candidate layout and would disable the SS16 / SS20 `<option>`s for an active `rs-block` text layer — but it never disables the *currently selected* size (only flags it with `#stoneSizeOverlapWarning`), and it is PERF-gated to re-run only on selection change or `#stoneSize` focus, not on a font switch. So reaching `rs-block` + SS16/SS20 by switching font on an existing SS16 layer, or by loading / importing a project, leaves overlapping stones with only a warning banner; the fix is a separate milestone.|
|The READ-011 rating pass returned no floor and the successor manipulation is unwritten|READ-011E|The 147-specimen READ-011 rating pass landed, but READ-011D's §6 selection rule could never adopt Form A — its ±0.25-stone tolerance was unsatisfiable against a single shared ratio cut grid spanning three regimes whose pool-median `stemWidthRatio` values span 3.05× — and Form B was power-capped: in `monoline\|outline\|untracked` only cut 16 ever reaches the 12-row clearance minimum, so the best Form B could return in outline is the value `MIN_HEIGHT_TO_STONE_RATIO` already holds. `MIN_HEIGHT_TO_STONE_RATIO` therefore remains **16 and warn-only**; no per-regime floor is shipped. The recommended successor is a different manipulation, not a re-fit — sample on the stones-across-stem axis directly with a per-regime ratio grid so every regime straddles the same candidate stone counts (READ-011E §10). Its design, cut grid and clearance rule belong to its own pre-registration milestone and are not yet written.|
|Three-letter interlocked script needs the `none` frame even at SS6 — with any frame it returns `CHAIN_TOO_THIN`, and every stone size above SS6 fails the `none` frame too|MONO-015 (frame clause added MONO-021)|**MONO-021 measurement (`develop` @ `abbd878`):** three-letter Great Vibes `"QWE"` at SS6 with `frameId: 'circle'` and the 150 mm cap FAILS `CHAIN_TOO_THIN` at 0.679 stems vs the 0.70 minimum — a frame steals the room the chain needs. The same `"QWE"` / SS6 with `frameId: 'none'` succeeds (365 stones). So the true rule is "three interlocked script letters need no frame at all, and only at SS6." Original finding (stone-size axis): The `'script'` layout's natural mark width scales linearly with the stone size (`singleChainHeightMm` is `0.85 · stoneSizeMm / stemWidthRatio`, and mark width is proportional to height). For Great Vibes "AKL" the natural padded mark is **136.5 mm at SS6** — inside the `none` frame's 150 mm cap (`COMMON_SCALING_LIMITS_MM` in `src/geometry/FrameLibrary.js`, applied to the Frame Size fields) — but **191.1 mm at SS10, 273.0 mm at SS16 (SS16 is 4.0 mm per `src/renderer/StoneSizes.js` — any table showing 3.8 mm is wrong), 320.8 mm at SS20, 436.8 mm at SS30**. Above SS6 the mark must shrink to fit, which drags the stem below the single-chain minimum, so "AKL" / SS10+ / `none` frame always fails. Documented in `docs/specifications/MONO-013-Interlock.md` §5 with no backlog entry until now. **MONO-015 weight sizing does not relieve it:** an assigned stone only widens the *bounding box*, through the per-stone half-diameter padding `StoneLayout.getBoundingBox()` adds (`src/geometry/StoneLayout.js`) — about 2 mm total for the two end stones — not the mark's point spread, which is fixed by the font geometry. The real fix (its own milestone) is either a larger frame allowance for the script layout or an authored-pitch relaxation; docs-only change here.|
|The `CHAIN_TOO_THIN` message quotes a non-monotonic, wrong stem-stone figure for OpenType slot-layout monograms — it reports the first sub-floor letter, not the binding one|MONO-016 (diagnosed MONO-017, resolved MONO-018)|**Resolved by MONO-018:** the single-chain minimum gate is hoisted out of `MonogramGenerator.generate()`'s per-letter OpenType loop. Every letter is now fitted before the outcome is decided, and on failure the **binding** letter is reported — the one with the minimum `achievedStemStones`, ties broken on the lower slot index. For Great Vibes `A,K,L` / `equal-three` / `none` / SS6 the reported figure is now `K` at 150×150 / 120×120 / 100×100 = 0.470 / 0.376 / 0.309 (strictly decreasing; the 0.591 up-tick is gone). The message gains a sub-floor count (`2 of 3 letters fall below`) and `diagnostics` gains `allLetterStemStones` (every letter's fitted stem, in slot order). `CHAIN_TOO_THIN` still emits `layers: null`, so no committed geometry baseline moved (MONO-012/013/014/015/016 suites re-run clean). `INVALID_FONT` and `FITTING_FAILED` inside the loop still fail fast — only the `CHAIN_TOO_THIN` gate defers. The rejected alternative (keep fail-fast, reword the number as one-of-several) and its cost figures are in `docs/specifications/MONO-018-BindingLetter.md`. **Diagnosed in `docs/specifications/MONO-017-StemReportDiagnosis.md` — reporting defect, not a fitting defect; no geometry is affected.** `MonogramGenerator.generate()`'s per-letter OpenType branch in `src/monogram/MonogramGenerator.js` (the `for` loop at line 722, gate + fail-fast `return` at lines 791-802) quotes `achievedStemStones` for the **first letter in slot-index order** below `minChainStones`, not the minimum across letters. Different letters shrink by different amounts to fit an identical slot, so the identity of the first sub-floor letter changes with frame size: for Great Vibes `A,K,L` / `equal-three` / `none` / SS6 / `letterSpacingMm` 0 the reported figure is **K 0.470 at 150×150, K 0.376 at 120×120, A 0.591 at 100×100** — non-monotone only because `A` (i=0) clears the 0.70 floor at 150/120 and drops below it at 100, taking over as the reported letter while the true worst letter (K, 0.310 at 100×100) is never evaluated. Every individual letter's fitted height is monotone in frame size; the fit loop converges in 1–5 of its 6 iterations and the earlier "landing on the iteration cap" hypothesis is disproved. `generate()` returns the correct `CHAIN_TOO_THIN` outcome and emits no geometry at every size — only the quoted number is wrong. Generalises to `two-letter` / `traditional-three` / `equal-three` with any OpenType font + ≥2 letters; not `script` (one joined string) and not authored fonts (no stem figure). Fix (its own milestone, message/diagnostics only, no baseline moves): report the binding letter — see MONO-017 §5.|
|Stamp / Trace / Eraser silently discard a mark placed on a non-path layer — blocks generate → edit in Design → apply|MONO-020A|**Resolved by RS-3015:** `resolveMarkTargetByBounds()` (`src/drawing/DrawingCanvasTool.js`) skips any board proxy whose `item.data.markEligible !== true` — `tagMarkTarget()` stamps `markEligible = (layer.type === 'path')` at all twelve layerId-stamp sites — and keeps walking down the stack, so a mark on a monogram's joined-string `text` proxy resolves the frame `path` beneath it. The silent discard is gone on all three tools: `onStampPlace` messages a null / non-`path` resolution (commit `e4957e4`), and Trace / Eraser fire `onTraceRejected(reason[, layerId])` / `onEraseRejected(reason)` — `'no-target'` / `'ineligible'` / `'no-stones'` (Trace only) — which `app.js` maps to one status string each (the third commit; `e4957e4`'s `if(!layerId)` branches in `onTracePlace` / `onEraseSweep` were unreachable and are removed). `hitTestShapeId()` is untouched; Select still hits every shape. See `docs/specifications/RS-3015-MarkTargetEligibility.md`. RS-3015 did **not** make text layers editable — a mark on a monogram letter resolves the frame *beneath* it, deliberately; making the letter itself a valid target is MONO-021 (row below). This unblocks MONO-021. Original finding: `resolveTargetLayerIdByBounds()` resolved a mark to the topmost layer proxy whose bounding box contained the point; `syncFromProjectLayers()` puts a proxy on that board for every `text` / `svg` / `image` / `circle` / `rectangle` / shape-library layer, not only `path` layers, so a mark dropped on one of those resolved that non-`path` layer and the three handlers required `type === 'path'` and `return`ed with no status message — the tool **silently discards a mark on a non-path layer**. A generated monogram was the first customer-visible case; not monogram-specific. Select was never affected — the earlier claim that `hitTestShapeId()` "selects nothing" on a monogram frame was refuted in the browser.|
|Monogram layer ids are deterministic, so regenerating the same frame+layout twice into one project collides|MONO-014|**Resolved by MONO-019:** the re-id happens at insertion, not in the generator. `generateMonogram()` in `app.js` rewrites each returned layer's `id` with one shared per-generation suffix — base-36 `Date.now()` plus a bounded counter, with the `monogram-` prefix shortened to `mono-` — after `generate()` returns and before `commitHistory()`. `MonogramGenerator.generate()` is untouched, so its output stays byte-identical to develop (the MONO-012/013/015/016/018 and `test-geometry-engine` determinism baselines depend on that). The emitted layers carry no baked stones (`test-mono-006` tests 8/10), so re-iding the layer object is enough — nothing downstream holds a stale id — and MONO-013's connected-script layer follows the same rule. Worst-case id length is asserted against `LAYER_ID_PATTERN` from the real catalogs in `tools/test-mono-019-layer-ids.mjs`. See `docs/specifications/MONO-019-LayerIds.md`. Original finding: `MonogramGenerator.generate()` builds every layer id from `frameId` / `layoutId` alone (`monogram-${frameId}-${layoutId}-frame`, `monogram-${frameId}-${layoutId}-letter-N`). Generating a second monogram with the same frame and layout into a project that already holds one produces duplicate layer ids; `validateProject()` (`app.js`, the normalization gate every imported/autosaved/recovered project passes through) rejects a project with non-unique layer ids, so the collision surfaces on the next autosave-recovery / import / save round-trip rather than at generation time. Latent since MONO-005/006 — the ids have always been frame+layout deterministic. Fix (its own milestone): give generated monogram ids a per-generation unique suffix (a short random token or a `project`-scoped counter), the same way every other internally generated layer id is a bare type name plus `Date.now()` digits (the SEC-001 convention documented at `app.js:987` — there is no `insertLibraryItem()` in `app.js`).|
|Every Design tool (Stamp / Trace / Eraser both modes / Paint) works directly on a `type: 'text'` layer, so a generated monogram can be edited in Design and applied to the mug|MONO-021|**In progress — `feature/mono-021-text-marks` off `develop` @ `abbd878`. See `docs/specifications/MONO-021-TextLayerEditing.md`.** RS-3015 delivered the fall-through past a text proxy and the "that layer can't take marks" status messages — it did **not** make text layers editable; a mark on a monogram letter still resolves the frame beneath. MONO-021 makes the letter itself the target. A `'text'` layer gains the same four optional fields a `'path'` layer already carries (`stampedStones` / `erasedGridPositions` / `regions` / `naturalBoundingBoxMm`), applied inside `GeometryEngine.generateTextLayout()` in the same `regions → erased → stamped` order `generatePathLayout()` uses, rooted in a frozen box (the AABB of the base text stones, captured once at the first edit — the RS-3014 Step 3 pattern) since a text layer has no `contours`. Nothing is flattened: the letter stays `type: 'text'`, retypable and regenerable. **Flatten was rejected on measured evidence** — script `"QW"` / Great Vibes / SS6 flattened + regenerated in outline mode gives 275 stones vs 279, only 35/279 (12.5%) within 0.001 mm of their original position (negative control, +3 mm shift: 2/279); the chain visibly re-phases exactly when the user asks to edit it. Two behaviours diverge from the path layer, both on measured reasons in the spec: **Paint on text is colour-only** (resample empties a one-stone-wide stroke — 0 placed through the real `_applyPathRegions()`; an in-place size change gives 53 overlapping pairs), and **the Outline eraser sweeps a corridor** rather than cutting `contours` (a text layer has no fill to reflow; cutting would move untouched beads — the flatten defect again). The stacking hazard the earlier row described is a bounding-box test (`resolveMarkTargetByBounds()` uses `item.bounds.contains()`) covering **25.8% of a 150 mm frame's box** for a script `"QW"` letter; MONO-021 answers it with proximity-to-a-real-bead resolution (`TEXT_MARK_PROXIMITY_FACTOR = 1.0` stone diameter). Not monogram-specific — built as a general text-layer capability, since Text is coming to the Design toolset as its own mode. `hasDesignAuthoredEdits()` (MONO-020, deliberately unbranched on `layer.type`) fires for an edited letter with no change, so a re-Generate releases the edited set and adds a new one.|
|Painting a text layer cannot change the stones' size — colour only — because chain thinning needs the touching threshold, not `stoneSize + gap`|MONO-021|MONO-021's Paint on a `'text'` layer recolours the base stones in place and does nothing else. A size change is not offered because both ways of delivering one fail on measured evidence (script `"QW"` / Great Vibes / SS6 / 279 beads, 36-bead lasso): **resample** through the real `_applyPathRegions()` removes 35 beads and places **0** (it clips region candidates to points inside the shape polygons, and a one-stone-wide stroke has almost none), and an **in-place restyle** to 3.2 mm gives **53 overlapping pairs, worst penetration 1.163 mm**. Thinning the chain to absorb the larger stone is the real answer, but a greedy drop rule keyed to `stoneSize + gap` fails its own negative control — run at the *original* stone size it removes 19 beads that should stay, because the shipped chain already holds neighbour pairs at 2.093 mm (tighter than `stoneSize + gap`). Correct thinning needs the touching threshold and its own goldens. Its own milestone.|
|Retire `rs-block` and `rs-modern` from the monogram font picker (keep the fonts for saved projects)|FONT-LIB-007|`GeometryEngine.resolveTextPolygons()` throws for `rs-block` / `rs-modern` (authored stone centres, no vector outline). MONO-021 is built so no code path needs a text layer's outline, so these two fonts keep working everywhere — but they are still offered in the monogram font picker, where authored-only fonts already get special handling (`MonogramGenerator` requires authored fonts for slot layouts; script needs a stem width). Retiring them from the *picker* only — the FONT-LIB-005 precedent (Montserrat retired from the picker, binary retained so saved projects render unchanged) — removes a confusing option. 16 monogram test files and 36 test files overall reference these two font ids and are **expected** to be unaffected because the engine keeps the fonts — that expectation must be verified, not assumed, when this is scoped. Not implemented in MONO-021.|
|Playwright is imported by eight repo files but is not declared in `package.json`|MONO-021|`tools/test-read-011c-tracking-solver-regression.mjs`, `tools/mono-014-screenshots.mjs`, `tools/mono-013-screenshots.mjs`, `tools/generate-rs-modern-qa-sheets.mjs`, `tools/generate-rs-block-qa-sheets.mjs`, `tools/font-certification/evaluate-source.mjs`, `tools/font-certification/lib/screenshotPages.mjs` and `tools/font-certification/certify.mjs` all `import ... from 'playwright'`, but it is in neither `dependencies` nor `devDependencies`. `npm ci` deletes it and every browser-verification / screenshot tool breaks with a bare module-resolution error. Use `npm install` (which leaves an already-present `playwright` alone), or add it as a `devDependency`. Decision, not a fix now.|
|Stamp on a `stonesGenerated: false` path layer places a stone with no ghost circle; Trace on the same layer refuses|MONO-021 (pre-dates RS-3015)|The Stamp ghost hover (`updateStampGhostItem`) shows nothing over a path layer whose stones are not generated yet, but the committed click still pushes a `stampedStones` entry, which renders once stones exist. Trace on the same layer fires `onTraceRejected('no-stones')` and points at the Generate Stones button. The two tools disagree about whether an un-generated shape can take a mark. Needs a decision (both refuse, or both accept), not a fix now.|

## Open blocking defects (not yet scoped)

_None. The mark-tool silent-discard defect (MONO-020A) that sat here is **resolved by RS-3015** — its row moved to "Deferred technical follow-ups" above._
